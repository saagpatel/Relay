# G3 Evidence Templates

Required files during enforced release runs:

- `sbom.spdx.json`
- `provenance.intoto.jsonl`
- `artifact-signature.txt`

Before signing credentials are available, this directory may only contain this
template file.
